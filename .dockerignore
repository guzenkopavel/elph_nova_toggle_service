# Dependencies
node_modules

# Build output (rebuilt inside Docker)
dist

# Git
.git
.gitignore

# Environment files (never bake secrets into image)
.env
.env.*
!.env.example

# Test artifacts
test-results
coverage
playwright-report

# Test source (not needed at runtime)
tests
playwright.config.ts

# Dev tooling
*.test.ts
vitest.config.ts
tsconfig.json

# macOS
.DS_Store

# Docs (optional — remove this line if you want docs in the image)
docs

# Scripts (not needed at runtime — sync-manifest is run manually)
# scripts
